# Environment Configuration Template
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env file to version control!

# ============================================
# JWT Authentication (REQUIRED)
# ============================================
# Generate a strong random key with at least 32 characters
# On Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# On Linux/Mac: openssl rand -base64 32
JWT_SECRET=REPLACE-WITH-SECURE-RANDOM-KEY-MIN-32-CHARS

# ============================================
# Server Configuration
# ============================================
# Port the server will listen on
PORT=7071

# Environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

# ============================================
# Optional: Azure Key Vault (Production)
# ============================================
# If using Azure Key Vault for secrets management
# AZURE_KEY_VAULT_URL=https://your-vault.vault.azure.net/

# ============================================
# Optional: Additional Security Settings
# ============================================
# Override default settings from appsettings.json if needed
# Security__JWT__TokenExpirationHours=24
# Security__JWT__ValidateAudience=true
# Security__JWT__ValidateIssuer=true

